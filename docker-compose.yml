version: "3"

services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        volumes:
            - ./backend:/app/backend
        ports:
            - "5000:5000"
